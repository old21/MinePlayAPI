export enum Bills {
  COINS,
  MONEY,
  KEYS,
  TOKENS,
}

export enum SystemBills {
  REALCOINS,
  GAMECOINS,
  MONEY,
  KEYS,
  TOKENS,
}

export enum ExchangeBills {
  COINS,
  KEYS,
  TOKENS,
}

export enum ReplenishTypes {
  PURCHASE,
  REFERRAL,
  REDEEM,
}